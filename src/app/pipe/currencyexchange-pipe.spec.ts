import { CurrencyexchangePipe } from './currencyexchange-pipe';

describe('CurrencyexchangePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyexchangePipe();
    expect(pipe).toBeTruthy();
  });
});
